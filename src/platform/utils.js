export function downloadJSON(name,data){let blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),500)}export function pick(a){return a[Math.floor(Math.random()*a.length)]}export function clean(w){return String(w||'').toLowerCase().replace(/[^a-z]/g,'')}export function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}export function opener(s){return clean(String(s).split(/\s+/).slice(0,3).join(''))}export function shuffle(a){return [...a].sort(()=>Math.random()-.5)}
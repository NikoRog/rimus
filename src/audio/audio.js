export function createAudioSystem({ els, assetPath }) {
  return {ctx:null,buf:null,volume:+(localStorage.reVolume||.8),async init(){if(this.ctx)return;try{this.ctx=new (window.AudioContext||window.webkitAudioContext)();let r=await fetch(assetPath),a=await r.arrayBuffer();this.buf=await this.ctx.decodeAudioData(a)}catch(e){}},set(v){this.volume=Math.max(0,Math.min(1,+v));localStorage.reVolume=this.volume;if(els.volumeSlider)els.volumeSlider.value=Math.round(this.volume*100);if(els.volumeLabel)els.volumeLabel.textContent=Math.round(this.volume*100)+'%'},yo(){this.init();if(!this.ctx||!this.buf)return this.beep(660,.06,'square');let s=this.ctx.createBufferSource(),g=this.ctx.createGain();g.gain.value=this.volume*1.7;s.buffer=this.buf;s.connect(g);g.connect(this.ctx.destination);s.start()},beep(f=440,d=.06,type='square'){if(!this.ctx)return;let o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.value=f;g.gain.value=this.volume*.55;o.connect(g);g.connect(this.ctx.destination);o.start();o.stop(this.ctx.currentTime+d)},pop(){this.beep(740,.07,'triangle')},lose(){this.beep(150,.16,'sawtooth')},end(){this.beep(880,.18,'triangle')}};
}
